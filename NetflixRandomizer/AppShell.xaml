<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NetflixRandomizer.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NetflixRandomizer"
    xmlns:views="clr-namespace:NetflixRandomizer.Views"
    xmlns:controls="clr-namespace:NetflixRandomizer.Views.Base"
    Title="NetflixRandomizer"
    FlyoutBackgroundColor="White" >

    <Shell.Resources>
        <Style x:Key="BaseStyle" TargetType="Element">
            <Setter Property="Shell.BackgroundColor" Value="#455A64" />
            <Setter Property="Shell.ForegroundColor" Value="White" />
            <Setter Property="Shell.TitleColor" Value="White" />
            <Setter Property="Shell.DisabledColor" Value="#B4FFFFFF" />
            <Setter Property="Shell.UnselectedColor" Value="#95FFFFFF" />
        </Style>
        <Style TargetType="ShellItem" BasedOn="{StaticResource BaseStyle}" />
        <Style x:Key="DomesticShell" TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor" Value="#039BE6" />
        </Style>
        <Style x:Key="MonkeysShell" TargetType="Element" BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor" Value="#689F39" />
        </Style>
        <Style x:Key="ElephantsShell" TargetType="Element" BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor" Value="#ED3B3B" />
        </Style>
        <Style x:Key="BearsShell" TargetType="Element" BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor" Value="#546DFE" />
        </Style>
        <Style x:Key="AboutShell" TargetType="Element" BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor" Value="#96d1ff" />
        </Style>
    </Shell.Resources>

    <Shell.FlyoutHeader>
        <controls:FlyoutHeader />
    </Shell.FlyoutHeader>

    <Shell.FlyoutFooter>
        <controls:FlyoutFooter />
    </Shell.FlyoutFooter>

    <FlyoutItem Title="Películas" Icon="logonetflix.png">
        <ShellContent Title="Películas" Style="{StaticResource DomesticShell}" ContentTemplate="{DataTemplate views:FilmsView}" Route="FilmsView"  />
    </FlyoutItem>

    <FlyoutItem Title="Película Aleatoria" Icon="logonetflix.png">
        <ShellContent Title="Película Aleatoria" Style="{StaticResource MonkeysShell}" ContentTemplate="{DataTemplate views:RandomFilmView}" Route="RandomFilmView"  />
    </FlyoutItem>


    <TabBar>
        <ShellContent Title="Películas" Icon="films_icon.png" ContentTemplate="{DataTemplate views:FilmsView}" Route="FilmsViewTab" />
        <ShellContent Title="Aleatorio" Icon="random_icon.png" ContentTemplate="{DataTemplate views:RandomFilmView}" Route="RandomFilmViewTab" />
    </TabBar>

</Shell>
